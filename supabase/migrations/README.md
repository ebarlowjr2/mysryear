# Supabase migrations

Place versioned SQL migrations in this folder.

We still need to import the Sprint 3 SQL migration (notifications + tracking + profile prefs)
from the prior sprint artifact. Once that script is located, add it here as a timestamped
migration file.
